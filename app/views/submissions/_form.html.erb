<%= form_with(model: submission) do |form| %>
  <div class="form-group">
    <%= form.text_field :word, class:"form-control", placeholder: "Enter string" %>
  </div>

  <div class="form-group">
    <%= form.submit 'Submit', class: "btn btn-word" %>
  </div>
<% end %>